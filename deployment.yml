apiVersion: apps/v1
kind: Deployment
metadata:
  name: mydeploy

spec:
  replicas: 3
  selector:
    matchLabels:
      app: latha

  template:
    metadata:
      labels:
        app: latha

    spec:
      containers:
      - name: cont-1
        image: kartheek0976/priya:one
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: mysvc
spec:
  type: LoadBalancer
  selector:
    app: latha
  ports:
  - port: 80
    targetPort: 8080
